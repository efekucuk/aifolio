# required: your groq api key from https://groq.com
# get one at https://groq.com and replace this with your actual key
GROQ_API_KEY=your_groq_api_key_here

# cors origins: in development, defaults to allow all origins (*)
# in production, change this to your actual domain(s)
ALLOWED_ORIGINS=*

# environment: keep as development for local testing
NODE_ENV=development

# port: default port for local development
PORT=3000

# note: for production deployment:
# 1. replace groq_api_key with your actual api key
# 2. change allowed_origins to your domain(s) e.g., https://yourdomain.com
# 3. set node_env=production 